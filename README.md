## next-prev-pagination(server-side)

Install the packages using 

#### -- npm install


Then start the application using

#### -- npm start 

The application will run on : http://localhost:3000/home
